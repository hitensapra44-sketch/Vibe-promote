You are building a React + Supabase app. Implement a “Connect Account” feature that works with Reddit and Product Hunt.

1. UI: Add a button group in src/components/results-tracker/ConnectAccounts.jsx. Each button opens a simple input prompt for the username.
2. On click, send the username to Supabase and store it in a new table `social_accounts` (user_id, platform, username, created_at).
3. Immediately fetch posts:
   • Reddit – GET https://www.reddit.com/user/{username}/submitted.json and extract title, score, num_comments, created_utc.
   • Product Hunt – call the GraphQL endpoint (use placeholder data if no token).
   Approximate calculations:
      - views = score * 3      - engagements = score
      - comments = num_comments
      - link_clicks = score * 0.2
4. Before inserting, delete any existing rows for the same user_id + platform in `social_posts`, then insert the new rows with fields: user_id, platform, title, views, engagements, comments, link_clicks, created_at.
5. After a successful fetch and insert, refresh the analytics data in ResultsTracker.jsx so the UI updates instantly.
6. Wrap all fetch logic in try/catch; on error show alert("Failed to fetch data. Try again.") and keep the app running.
7. While fetching, disable the button or show “Connecting…”.
8. Do NOT add new libraries, do NOT change existing UI components, do NOT implement OAuth. Keep everything minimal and production‑safe.
9. Provide only the modified files: ConnectAccounts.jsx and ResultsTracker.jsx (or equivalents). Do not rename existing components.
10. Return the full code for the changed files using <dyad-write> tags, and include the SQL to create `social_accounts` and any needed column additions to `social_posts`.

Make sure the code uses the existing supabase client from src/supabaseClient.js and follows the existing project structure.