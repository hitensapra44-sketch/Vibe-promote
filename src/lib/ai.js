import { OpenAI } from 'openai'; // Using absolute URL for the proxy to satisfy OpenAI client validation const client = new OpenAI({ apiKey: "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", baseURL: `${window.location.origin}/api/ai`, dangerouslyAllowBrowser: true }); /** * Generates a structured set of keywords for audience targeting based on the brand brain. * This is used by the Audience Spotter to find high-intent conversations. * Returns exactly 10 sub-keywords as required. */ export const generateAudienceKeywords = async (brandBrain) => { const systemPrompt = `You are an elite Growth Marketing Strategist specializing in community-led growth on Reddit and Hacker News. Analyze this SaaS product and identify exactly where its buyers hang out and what phrases they use when actively searching for solutions. Return ONLY a JSON object with these exact fields: { "sub_keywords": [Array of EXACTLY 10 highly-specific, intent-driven search phrases] } RULES FOR SUB-KEYWORDS (10 total, no more, no less): 1. Primary Intent (2-3): What users are actively searching for to solve their problem. 2. Pain-Point Keywords (2-3): Reflect user frustrations or goals. 3. Contextual Keywords (2-2): Situational or use-case based. 4. Intent-Based Variations (2-3): Mix of informational, problem-aware, and solution-aware. 5. Platform-Specific (1-2): Tied to specific platforms. 6. Long-Tail Priority: All phrases must be multi-word, highly specific, realistic search queries. 7. NO repetition or slight variations of the same phrase. 8. Each keyword must reflect REAL search/query behavior. 9. NO generic terms like "marketing tool" or "SaaS software". 10. Focus on maximizing relevance for discovering the target audience. Format: Return ONLY valid JSON. No markdown, no extra text.`; const userMessage = ` App Name: ${brandBrain.app_name} Description: ${brandBrain.app_description} Target Customer: ${brandBrain.target_customer} Core Problem: ${brandBrain.core_problem} Unique Advantage: ${brandBrain.unique_differentiator} Brand Tone: ${brandBrain.brand_tone} Writing Style: ${brandBrain.writing_style} Primary Platform: ${brandBrain.primary_platform} Posting Frequency: ${brandBrain.posting_frequency} `; try { const completion = await client.chat.completions.create({ model: "nvidia/nemotron-mini-4b-instruct", messages: [ { role: "system", content: systemPrompt }, { role: "user", content: userMessage } ], temperature: 0.2, top_p: 0.7, max_tokens: 1024, stream: false }); if (!completion.choices || !completion.choices[0] || !completion.choices[0].message) { throw new Error("AI returned an empty response."); } let content = completion.choices[0].message.content; // Clean up any potential markdown formatting content = content.replace(/```json\n?|```/g, '').trim(); return JSON.parse(content); } catch (err) { console.error("AI Keyword Generation Error:", err.message); if (err.message.includes('404')) { throw new Error("Proxy not active. Please click the 'Restart' button above."); } throw err; } }; export const generateAICall = async (systemPrompt, userMessage) => { try { const completion = await client.chat.completions.create({ model: "nvidia/nemotron-mini-4b-instruct", messages: [ { role: "system", content: systemPrompt }, { role: "user", content: userMessage } ], temperature: 0.2, top_p: 0.7, max_tokens: 1024, stream: false }); if (!completion.choices || !completion.choices[0] || !completion.choices[0].message) { throw new Error("AI returned an empty response."); } let content = completion.choices[0].message.content; // Clean up any potential markdown formatting content = content.replace(/```json\n?|```/g, '').trim(); return content; } catch (err) { console.error("AI Call Error:", err.message); if (err.message.includes('404')) { throw new Error("Proxy not active. Please click the 'Restart' button above."); } throw err; } };